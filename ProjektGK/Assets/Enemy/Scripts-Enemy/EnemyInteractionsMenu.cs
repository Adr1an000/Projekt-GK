using System.Collections;
using System.Collections.Generic;
using UnityEngine;

    public enum EnemyBehaviour
    {
        IDLE_STATE = 0,
        PATROL_STATE = 1,
        CHASE_STATE = 2,
        ATTACK_STATE = 3,
        RETREAT_STATE = 4
    }

